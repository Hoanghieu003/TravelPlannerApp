query getWriteBlogTags($member_code: Int, $tag_type: Int, $language: String!) {
  getWriteBlogTags(
    member_code: $member_code
    tag_type: $tag_type
    language: $language
  ) {
    tags {
      code
      type

      translations {
        name
        language
      }
    }
  }
}
